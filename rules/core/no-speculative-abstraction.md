# core.no-speculative-abstraction

Do not add factories, interfaces, extension points, or frameworks for future needs that are not present.

Add structure only when it removes real current complexity.
