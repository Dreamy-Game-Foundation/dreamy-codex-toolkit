# unity.lifecycle

Use `Awake`, `OnEnable`, `Start`, `OnDisable`, and `OnDestroy` according to ownership and dependency timing.

Do not move subscriptions, async starts, or service registration without checking lifecycle order.
