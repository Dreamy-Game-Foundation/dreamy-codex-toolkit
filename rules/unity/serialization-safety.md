# unity.serialization-safety

Serialized field, type, namespace, and asmdef changes require a migration path.

Use Unity-supported preservation such as `FormerlySerializedAs` when renaming serialized fields.
