# Addressables Policy

Project owns groups, profiles, labels, remote content, and build configuration. Runtime helpers may be packaged. Check download size/progress/failure for optional remote content and avoid string addresses scattered across leaves.
