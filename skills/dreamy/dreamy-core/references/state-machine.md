# State Machine

Use a state machine for mutually exclusive runtime states with guarded transitions, entry cleanup, and exit cleanup. Do not introduce a state machine for simple booleans. Verify invalid transition behavior and duplicate enter/exit calls.
