# Tick Service

Use a tick service only when it reduces scattered Update loops and centralizes a real lifecycle/budget. Name tick owner, frequency, pause behavior, subscription cleanup, and profiling budget.
