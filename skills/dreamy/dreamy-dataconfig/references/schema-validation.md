# DataConfig Schema Validation

Validate required fields, duplicate IDs, missing references, invalid enum/key values, and fallback/default policy before runtime consumers depend on config. Missing production config should be visible, not silently hidden in each UI panel.
