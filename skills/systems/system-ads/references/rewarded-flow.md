# Rewarded Ads

A rewarded ad reward is granted only after a verified reward callback. Show success, ad opened, or ad closed is not a grant. Duplicate callbacks use idempotency and persist before analytics success.
