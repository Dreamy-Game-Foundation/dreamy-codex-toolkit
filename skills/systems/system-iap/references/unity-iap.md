# Unity IAP

Treat store product availability, pending purchases, callbacks, receipts, and restore as separate states. UI is never the purchase source of truth. Compatibility and installed package version must be inspected before API claims.
